@using Microsoft.AspNetCore.Components.Authorization
@using CoreBusiness.Utils
@inject NavigationManager Navigation
@inject AuthenticationStateProvider AuthenticationStateProvider
<nav class="navbar shadow-sm d-lg-flex flex-column p-0 position-relative">
    <div class="container-fluid d-flex justify-content-between align-items-center p-2">
        <a class="navbar-brand fw-semibold text-white" href="">HajjManagement</a>

        <!-- Hamburger button for small screens -->
        <button class="navbar-toggler d-lg-none" type="button" data-bs-toggle="collapse" data-bs-target="#sidebarMenu" aria-controls="sidebarMenu" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>

    <!-- Sidebar -->
    <div class="collapse d-lg-block sidebar w-100" id="sidebarMenu">
        <AuthorizeView>
            <Authorized Context="cntxt">
                <nav class="nav flex-column p-2">

                <!-- Home -->
                <AuthorizeView>
                    <Authorized>
                    <div class="nav-item">
                        <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" href="/home" Match="NavLinkMatch.All">
                            <i class="bi bi-house-door-fill text-white"></i>
                            <span class="text-white">الرئيسية</span>
                        </NavLink>
                    </div>
                    </Authorized>
                    <NotAuthorized></NotAuthorized>
                </AuthorizeView>

                    <!-- Home -->
                    <AuthorizeView Roles="@( $"{RoleNames.MainSuperAdmin},{RoleNames.MainAdmin},{RoleNames.CountrySuperAdmin},{RoleNames.CountryAdmin},{RoleNames.CountrySupervisor},{RoleNames.CompanySuperAdmin},{RoleNames.CompanyAdmin}")">
                        <Authorized>
                            <div class="nav-item">
                                <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" href="/dashboard" Match="NavLinkMatch.All">
                                    <i class="bi bi-speedometer2 text-white"></i>
                                    <span class="text-white">Dashboard</span>
                                </NavLink>
                            </div>
                        </Authorized>
                        <NotAuthorized></NotAuthorized>
                    </AuthorizeView>
                    <!-- Countries -->
                    <AuthorizeView Roles="@( $"{RoleNames.MainSuperAdmin},{RoleNames.MainAdmin}" )">
                        <Authorized>
                            <div class="nav-item">
                                <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" href="Country/List">
                                    <i class="bi bi-flag text-white"></i>
                                    <span class="text-white">الدول</span>
                                </NavLink>
                            </div>
                        </Authorized>
                        <NotAuthorized></NotAuthorized>
                    </AuthorizeView>

                    <!-- Administrative Structure -->
                    <AuthorizeView Roles="@($"{RoleNames.MainSuperAdmin},{RoleNames.MainAdmin}")">
                        <Authorized>
                            <div class="nav-item">
                                <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" href="CountryStructure/List">
                                    <i class="bi bi-diagram-3 text-white"></i>
                                    <span class="text-white">مسميات الهيكل الإداري</span>
                                </NavLink>
                            </div>
                        </Authorized>
                        <NotAuthorized></NotAuthorized>
                    </AuthorizeView>

                    <!-- Administrative Division -->
                    <AuthorizeView Roles="@($"{RoleNames.MainSuperAdmin},{RoleNames.MainAdmin},{RoleNames.CountrySuperAdmin},{RoleNames.CountryAdmin}")">
                        <Authorized>
                            <div class="nav-item">
                                <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" href="AdministrativeDivision/List">
                                    <i class="bi bi-diagram-3-fill text-white"></i>
                                    <span class="text-white">الهيكل الإداري</span>
                                </NavLink>
                            </div>
                        </Authorized>
                        <NotAuthorized>

                        </NotAuthorized>
                    </AuthorizeView>


                    <!-- Bag -->
                    <AuthorizeView Roles="@($"{RoleNames.MainSuperAdmin},{RoleNames.MainAdmin}")">
                        <Authorized>
                            <div class="nav-item">
                                <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" href="bag/List">
                                    <i class="bi bi-briefcase-fill text-white"></i>
                                    <span class="text-white">الحقيبة</span>
                                </NavLink>
                            </div>
                        </Authorized>
                        <NotAuthorized></NotAuthorized>
                    </AuthorizeView>

                    <!-- Contracts -->
                    <AuthorizeView Roles="@($"{RoleNames.MainSuperAdmin},{RoleNames.MainAdmin}")">
                        <Authorized>
                            <div class="nav-item">
                                <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" href="Contract/List">
                                    <i class="bi bi-file-earmark-text-fill text-white"></i>
                                    <span class="text-white">العقود</span>
                                </NavLink>
                            </div>
                        </Authorized>
                        <NotAuthorized></NotAuthorized>
                    </AuthorizeView>

                    <!-- Guests -->
                    <div class="nav-item">
                        <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" href="Guest/List">
                            <i class="bi bi-person-lines-fill text-white"></i>
                            <span class="text-white">الزوار</span>
                        </NavLink>
                    </div>

                    <!-- Hotels -->
                    <div class="nav-item">
                        <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" href="Hotel/List">
                            <i class="bi bi-building text-white"></i>
                            <span class="text-white">الفنادق</span>
                        </NavLink>
                    </div>

                    <!-- User Management -->
                    @* <AuthorizeView Roles="MainSuperAdmin,MainAdmin,CompanySuperAdmin,CompanyAdmin,SuperAdmin,Admin">
                        <Authorized>
                            <div class="nav-item">
                                <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" href="User/List">
                                    <i class="bi bi-people-fill text-white"></i>
                                    <span class="text-white">إدارة المستخدمين</span>
                                </NavLink>
                            </div>
                        </Authorized>
                    </AuthorizeView> *@


                    <!-- Security Management -->
                    <AuthorizeView Roles="MainSuperAdmin,MainAdmin,CompanySuperAdmin,CompanyAdmin,SuperAdmin,Admin">
                        <Authorized>
                            <div class="nav-item">
                                <!-- Parent toggle -->
                                <a class="nav-link d-flex align-items-center justify-content-between gap-2"
                                   data-bs-toggle="collapse"
                                   href="#securityMenu"
                                   role="button"
                                   aria-expanded="false"
                                   aria-controls="securityMenu">
                                    <i class="bi bi-shield-lock text-white"></i>
                                    <span class="text-white">إدارة الأمان</span>
                                    <i class="bi bi-caret-down-fill text-white ms-auto small"></i>
                                </a>

                                <!-- Collapsible submenu -->
                                <div class="collapse ps-4" id="securityMenu" data-bs-parent="#sidebarMenu">
                                    <NavLink class="nav-link text-white-50 small d-block py-1" href="Role/List">
                                        <i class="bi bi-person-gear me-2"></i> الصلاحيات
                                    </NavLink>
                                    <NavLink class="nav-link text-white-50 small d-block py-1" href="User/List">
                                        <i class="bi bi-people me-2"></i> المستخدمين
                                    </NavLink>
                                </div>
                            </div>
                        </Authorized>
                    </AuthorizeView>


                    <!-- Change Password -->
                    <div class="nav-item mt-2 border-top pt-2">
                        <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" href="User/ChangePassword">
                            <i class="bi bi-key-fill text-white"></i>
                            <span class="text-white">تغيير كلمة المرور</span>
                        </NavLink>
                    </div>
                    <!-- Change Password -->
                    <div class="nav-item mt-2 pt-2">
                        <NavLink class="nav-link d-flex align-items-center justify-content-between gap-2" @onclick="Logout">
                            <i class="bi bi-door-open text-white"></i>
                            <span class="text-white">تسجيل خروج</span>
                        </NavLink>
                    </div>
                </nav>
            </Authorized>
        </AuthorizeView>
    </div>
</nav>
@code {
    private AuthenticationState AuthState;
    protected override async Task OnInitializedAsync()
    {
        AuthState = await ((CustomAuthStateProvider)AuthenticationStateProvider).GetAuthenticationStateAsync();
    }
    private async Task Logout()
    {

        // Sign out using the authentication scheme (default is Cookies)
        ((CustomAuthStateProvider)AuthenticationStateProvider).NotifyUserLogout();




        // Redirect to login page
        Navigation.NavigateTo("/", forceLoad: true);
    }
}